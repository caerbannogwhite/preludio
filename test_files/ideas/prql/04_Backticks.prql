
# Backticks
# https://prql-lang.org/book/queries/syntax.html#backticks
prql dialect:mysql
from employees
select `first name`

# prql dialect:postgres
from employees
select `first name`

# prql dialect:bigquery
from `project-foo.dataset.table`
join `project-bar.dataset.table` [col_bax]


# Strings
from a
filter b == 'hello'
derive c = "world"
